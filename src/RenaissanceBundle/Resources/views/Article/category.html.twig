{% extends "::base.html.twig" %}

{% block title %}RenaissanceBundle:Article:Index{% endblock %}

{% block body %}
<h1>Welcome to the Article:Index page</h1>

<a href="{{ path('_index') }}">back to list</a>

<h2>Artykuły z kategorii...</h2>

{% for item in articles %}
    <p>
    <a href="{{ path('_index') }}{{ item.slug }}/{{ item.slug }}">
        {{ item.slug }}
    </a> #{# item.getIdArticleCategory() #}
    </p>

{% endfor %}

{% endblock %}
